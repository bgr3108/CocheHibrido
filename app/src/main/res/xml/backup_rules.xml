<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Only persistent user data is backed up on Android 11 and lower. -->
    <!-- The database directory includes Room's main, WAL, and shared-memory files. -->
    <include domain="database" path="." />
    <include domain="file" path="datastore/vehicle_preferences.preferences_pb" />
</full-backup-content>
